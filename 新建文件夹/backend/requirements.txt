# ── Core ───────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# ── Phase 4: AI Services ───────────────────────────────────────

# Azure Speech Services (ASR + optional TTS)
azure-cognitiveservices-speech>=1.35.0

# OpenAI GPT-4o (LLM + Function Calling)
openai>=1.12.0

# Edge TTS (free TTS fallback, no API key needed)
edge-tts>=6.1.0

# Environment variable loading
python-dotenv>=1.0.0

# ── Optional ────────────────────────────────────────────────────
# vosk>=0.3.45           # server-side wake word verification
# ffmpeg-python>=0.2.0   # alternative to subprocess ffmpeg
